openapi: 3.0.0
info:
  title: refComponent
  description: refComponent
  version: 1.0.0
servers:
  - url: 'https'
paths:

components:
  schemas:
    NestedComponent:
      type: "object"
      properties:
        id:
          type: "integer"
          format: "int64"
        shipDate:
          type: "string"
          format: "date-time"
        category:
          $ref: "#/definitions/Category"
      xml:
        name: "Order"
    Category:
      type: object
      properties:
        name:
          type: string
        tag:
          $ref: "#/definitions/Tag"
    Tag:
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string





